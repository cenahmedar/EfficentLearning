<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.cenah.efficentlearning">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/CustomNoActionBar"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".teacher.fragments.TeachserStudentActivity"></activity>
        <activity android:name=".teacher.fragments.TeacherClassActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".teacher.activites.TeacherClassesActivity" />
        <activity android:name=".teacher.activites.TeacherHomeActivity" />
        <activity android:name=".student.activities.StudentHomeActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".admin.activities.AdminCourseActivity" />
        <activity android:name=".admin.activities.AdminTeacherActivity" />
        <activity android:name=".admin.activities.AdminUserActivity" />
        <activity android:name=".admin.activities.AdminStudentActivity" />
        <activity android:name=".admin.activities.AdminHomeActivity" />
        <activity android:name=".LoginActivity" />
    </application>

</manifest>